#include "Upgrade.h"

Upgrade::Upgrade(sf::Shape* objectShape, sf::Vector2f originPosition)
    : Object(objectShape, originPosition, sf::Vector2f(0.f, 0.f), 0.f, 0.f) {
    // Initialize the upgrade object
}